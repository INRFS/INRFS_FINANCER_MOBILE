# Android emulator uses 10.0.2.2 automatically. For a physical device, use the
# development computer's LAN address; production builds should use the HTTPS API.
EXPO_PUBLIC_API_BASE_URL=https://app.inrfs.com/financer-api/api/v1
